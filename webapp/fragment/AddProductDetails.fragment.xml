<core:FragmentDefinition xmlns="sap.uxap"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:m="sap.m"
    xmlns:f="sap.f"
    xmlns:core="sap.ui.core"
    xmlns:form="sap.ui.layout.form">
    <ObjectPageLayout id="ProductObjectPageLayout" showTitleInHeaderContent="true" alwaysShowContentHeader="false" preserveHeaderStateOnScroll="false" headerContentPinnable="true" isChildPage="true" upperCaseAnchorBar="false">
        <headerTitle>
            <ObjectPageDynamicHeaderTitle>
                <expandedHeading>
                    <m:Title text="{AppState>/oSelectedProduct/id}" wrapping="true" visible="false" />
                </expandedHeading>
                <actions>
                    <m:Button id="saveClientDetails" text="Save" icon="sap-icon://save" type="Critical" press="onSaveProductDetails" />

                    <m:Button icon="sap-icon://decline" type="Critical" press="onCloseDetailPage" />
                    <m:Button id="deleteProject" icon="sap-icon://delete" type="Critical" press="onDeleteProduct" />
                </actions>
            </ObjectPageDynamicHeaderTitle>
        </headerTitle>

        <headerContent>
            <m:FlexBox wrap="Wrap" fitContainer="true" alignItems="Stretch" />
        </headerContent>

        <sections>
            <ObjectPageSection title="Services Details">
                <subSections>
                    <ObjectPageSubSection>
                        <blocks>
                           <form:SimpleForm editable="true" layout="ResponsiveGridLayout">
                                <form:content>
                                    <m:Label text="Service Name" required="true"/>
                                    <m:Input value="{serviceModel>/name}" maxLength="100"/>
                                    
                                    <m:Label text="Category" required="true"/>
                                    <m:ComboBox
                                        selectedKey="{serviceModel>/categoryid}"
                                        items="{
                                            path: '/ServiceCategories',
                                            sorter: { path: 'name' }
                                        }">
                                        <core:Item key="{id}" text="{name}"/>
                                    </m:ComboBox>
                                    
                                    <m:Label text="Description"/>
                                    <m:TextArea value="{serviceModel>/description}" rows="3" growing="true"/>
                                </form:content>
                            </form:SimpleForm>

                        </blocks>
                    </ObjectPageSubSection>

 <ObjectPageSubSection>
                        <blocks>
                        <form:SimpleForm editable="true" layout="ResponsiveGridLayout">
                                <form:content>
                                    <m:Label text="Rate Type" required="true"/>
                                    <m:Select
                                        selectedKey="{serviceModel>/rate_type}"
                                        forceSelection="false">
                                        <core:Item key="hourly" text="Hourly"/>
                                        <core:Item key="daily" text="Daily"/>
                                        <core:Item key="project" text="Project-based"/>
                                        <core:Item key="retainer" text="Retainer"/>
                                        <core:Item key="unit" text="Per Unit"/>
                                    </m:Select>
                                    
                                    <m:Label text="Rate" required="true"/>
                                    <m:HBox>
                                        <m:Input 
                                            value="{serviceModel>/rate}" 
                                            type="Number"
                                            width="10rem"
                                            class="sapUiSmallMarginEnd"/>
                                        <m:ComboBox
                                            selectedKey="{serviceModel>/currency}"
                                            items="{
                                                path: 'supportedCurrencies>/'
                                            }"
                                            width="8rem">
                                            <core:Item key="{key}" text="{text}"/>
                                        </m:ComboBox>
                                    </m:HBox>
                                    
                                    <m:Label 
                                        text="{= ${serviceModel>/rate_type} === 'project' ? 'Estimated Hours' : 'Minimum Hours' }"
                                        visible="{= ${serviceModel>/rate_type} === 'project' || ${serviceModel>/rate_type} === 'hourly' }"/>
                                    <m:StepInput
                                        value="{serviceModel>/estimated_hours}"
                                        min="1"
                                        max="1000"
                                        step="1"
                                        width="10rem"
                                        visible="{= ${serviceModel>/rate_type} === 'project' || ${serviceModel>/rate_type} === 'hourly' }"/>
                                </form:content>
                            </form:SimpleForm>
                        </blocks>
                    </ObjectPageSubSection>

                     <ObjectPageSubSection>
                        <blocks>
                           <form:SimpleForm editable="true" layout="ResponsiveGridLayout">
                                <form:content>
                                    <m:Label text="Active Status"/>
                                    <m:Switch 
                                        state="{serviceModel>/is_active}"
                                        customTextOn="Active"
                                        customTextOff="Inactive"/>
                                    
                                    <m:Label text="Lead Time" tooltip="Time required before starting this service"/>
                                    <m:Input 
                                        value="{serviceModel>/lead_time}"
                                        placeholder="e.g., 2 weeks, 3 days"
                                        width="12rem"/>
                                </form:content>
                            </form:SimpleForm>
                        </blocks>
                    </ObjectPageSubSection>


                </subSections>
            </ObjectPageSection>
        </sections>
    </ObjectPageLayout>
</core:FragmentDefinition>